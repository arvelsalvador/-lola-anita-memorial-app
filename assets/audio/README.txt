The Highlights slideshow automatically picks up the first audio file in
this folder (`.mp3`, `.wav`, or `.m4a`) and plays it on a loop at a soft
volume with a mute toggle.

Example: `Kiss the Rain - Yiruma.mp3`

If the folder contains no audio, the slideshow simply runs silently and
the mute button is hidden.